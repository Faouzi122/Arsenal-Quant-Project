{
  "$schema": "https://static.modelcontextprotocol.io/schemas/v1/server-card.schema.json",
  "serverInfo": {
    "name": "Antigravity Engine - Decision Layer",
    "version": "1.0.0",
    "description": "AI Cost Intelligence and Autonomous Agent Loop Circuit Breaker Gateway, reachable at https://api.arsenal-quant.com",
    "endpoint": "https://api.arsenal-quant.com"
  },
  "authentication": {
    "required": true,
    "type": "L402",
    "description": "Lightning Network micro-payment authentication required for full diagnostic data and actionable recommendations."
  },
  "resources": [
    {
      "uri": "mcp://audit/latest",
      "name": "Latest AI Cost Intelligence Audit",
      "description": "Diagnostic and financial teaser report of detected agent loop inefficiencies.",
      "mimeType": "text/plain"
    }
  ],
  "tools": [
    {
      "name": "circuit_breaker",
      "description": "Terminates a runaway agent process or loop based on PID to stop token drain.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "action": { "type": "string", "enum": ["TERMINATE_AND_REGROUP", "MONITOR"] },
          "target_pid": { "type": "string" }
        },
        "required": ["action", "target_pid"]
      }
    },
    {
      "name": "cache_manager",
      "description": "Enables semantic cache to reduce redundant vector DB calls.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "action": { "type": "string", "enum": ["ENABLE_SEMANTIC_CACHE", "DISABLE_SEMANTIC_CACHE"] }
        },
        "required": ["action"]
      }
    }
  ],
  "prompts": []
}
